<section class="story-section">
  <div class="story-section__inner">
    <!-- Visuel -->
    <div class="story-section__visual">
      <img
        class="story-section__image"
        src="/images/home/happyclicks-demarche.webp"
        alt="Carnet de conception avec croquis de site web, notes et espace de travail créatif"
      />

      <span
        class="story-section__shadow story-section__shadow--one"
        aria-hidden="true"></span>

      <span
        class="story-section__shadow story-section__shadow--two"
        aria-hidden="true"></span>
    </div>

    <!-- Texte -->
    <div class="story-section__content">
      <p class="story-section__eyebrow">Ma démarche</p>

      <h2 class="story-section__title">
        Votre histoire<br />
        est unique.<br />

        <span>
          Votre site aussi<span class="story-section__dot">.</span>
        </span>
      </h2>

      <p class="story-section__text">
        Je prends le temps de comprendre votre projet, vos valeurs et vos
        objectifs pour imaginer un site qui vous ressemble vraiment et qui fait
        la différence.
      </p>

      <a class="story-section__link" href="/a-propos">
        <span>Et la vôtre ?</span>
        <span aria-hidden="true">→</span>
      </a>
    </div>
  </div>
</section>

<style>
  .story-section {
    position: relative;

    overflow: hidden;

    background: linear-gradient(
      180deg,
      rgba(229, 243, 241, 0.32),
      rgba(250, 247, 240, 0)
    );

    border-top: 1px solid rgba(11, 37, 48, 0.05);
  }

  .story-section__inner {
    width: min(calc(100% - 2rem), var(--content-max));

    margin-inline: auto;

    padding-block: clamp(4.5rem, 9vw, 8rem);

    display: grid;

    gap: 3rem;
  }

  /* ========================================
     VISUEL
     ======================================== */

  .story-section__visual {
    position: relative;

    min-width: 0;
  }

  .story-section__image {
    position: relative;
    z-index: 2;

    display: block;

    width: 100%;
    aspect-ratio: 4 / 3;

    object-fit: cover;
    object-position: center;

    border-radius: 22px 22px 60px 22px;

    box-shadow: 0 18px 45px rgba(11, 37, 48, 0.07);
  }

  /* Ombres végétales provisoires */
  .story-section__shadow {
    position: absolute;

    z-index: 1;

    display: block;

    width: 160px;
    height: 260px;

    border-radius: 50%;

    background: rgba(11, 37, 48, 0.06);

    filter: blur(28px);

    pointer-events: none;
  }

  .story-section__shadow--one {
    top: -50px;
    right: -40px;

    transform: rotate(-22deg);
  }

  .story-section__shadow--two {
    bottom: -80px;
    left: -60px;

    width: 120px;
    height: 210px;

    transform: rotate(18deg);
  }

  /* ========================================
     TEXTE
     ======================================== */

  .story-section__content {
    position: relative;

    z-index: 3;

    align-self: center;
  }

  .story-section__eyebrow {
    margin-bottom: 1rem;

    color: var(--color-teal);

    font-size: 0.72rem;
    font-weight: 600;

    letter-spacing: 0.09em;

    text-transform: uppercase;
  }

  .story-section__title {
    font-size: clamp(2.6rem, 5vw, 4.7rem);

    line-height: 0.98;

    letter-spacing: -0.03em;
  }

  .story-section__title > span {
    color: var(--color-teal);
  }

  .story-section__dot {
    color: var(--color-coral);
  }

  .story-section__text {
    max-width: 470px;

    margin-top: 2rem;

    font-size: clamp(0.98rem, 1.2vw, 1.08rem);

    line-height: 1.7;
  }

  /* ========================================
     LIEN
     ======================================== */

  .story-section__link {
    position: relative;

    display: inline-flex;
    align-items: center;

    gap: 0.9rem;

    margin-top: 2rem;

    padding-bottom: 0.4rem;

    font-family: var(--font-heading);

    font-size: 1.3rem;

    font-style: italic;
  }

  .story-section__link::after {
    content: "";

    position: absolute;

    left: 0;
    bottom: 0;

    width: 72%;
    height: 1px;

    background: var(--color-teal);

    transform-origin: left;

    transition: width var(--transition-base);
  }

  .story-section__link:hover::after,
  .story-section__link:focus-visible::after {
    width: 100%;
  }

  .story-section__link span:last-child {
    color: var(--color-teal);

    transition: transform var(--transition-base);
  }

  .story-section__link:hover span:last-child,
  .story-section__link:focus-visible span:last-child {
    transform: translateX(5px);
  }

  /* ========================================
     DESKTOP
     ======================================== */

  @media (min-width: 768px) {
    .story-section__inner {
      width: min(calc(100% - 4rem), var(--content-max));

      grid-template-columns:
        minmax(0, 1.08fr)
        minmax(0, 0.92fr);

      align-items: center;

      gap: clamp(3rem, 7vw, 7rem);
    }
  }

  /* ========================================
     MOBILE
     ======================================== */

  @media (max-width: 767px) {
    .story-section__inner {
      padding-block: 4.5rem;
    }

    .story-section__visual {
      order: 2;
    }

    .story-section__content {
      order: 1;
    }

    .story-section__title {
      font-size: clamp(2.7rem, 13vw, 3.8rem);
    }

    .story-section__image {
      border-radius: 18px 18px 44px 18px;
    }
  }
</style>
