<section class="values-section" id="benefices">
  <div class="values-section__inner">
    <header class="values-section__header">
      <p class="values-section__eyebrow">Un site utile</p>

      <h2 class="values-section__title">
        Votre site peut travailler<br />
        <span>
          pour vous<span class="values-section__dot">.</span>
        </span>
      </h2>

      <p class="values-section__intro">
        Un bon site ne se contente pas d’exister. Il soutient votre activité,
        rassure vos visiteurs et les aide à passer à l’action.
      </p>
    </header>

    <div class="values-section__grid">
      <article class="value">
        <h3 class="value__title">Être trouvé<span class="value__dot">.</span></h3>
        <p class="value__text">
          Une structure claire, des contenus pertinents et de bonnes bases pour
          le référencement, notamment local.
        </p>
      </article>

      <article class="value">
        <h3 class="value__title">Inspirer confiance<span class="value__dot">.</span></h3>
        <p class="value__text">
          Une présence professionnelle et cohérente qui rassure dès les
          premières secondes.
        </p>
      </article>

      <article class="value">
        <h3 class="value__title">Donner envie d’agir<span class="value__dot">.</span></h3>
        <p class="value__text">
          Une navigation fluide et des appels à l’action pensés pour transformer
          les visites en demandes de contact.
        </p>
      </article>

      <article class="value">
        <h3 class="value__title">Gagner du temps<span class="value__dot">.</span></h3>
        <p class="value__text">
          Des fonctionnalités choisies selon votre activité, sans accumulation
          d’options inutiles.
        </p>
      </article>
    </div>

    <div class="values-section__thread" aria-hidden="true">
      <svg viewBox="0 0 1200 100" preserveAspectRatio="none">
        <path
          d="
            M -40 52
            C 110 18, 205 78, 355 50
            C 470 29, 540 27, 620 51
            C 663 64, 690 77, 715 55
            C 739 33, 706 21, 688 40
            C 666 63, 706 77, 759 55
            C 910 4, 1030 83, 1240 43
          "
        ></path>
      </svg>
    </div>
  </div>
</section>

<style>
  .values-section {
    position: relative;
    overflow: hidden;
    background: var(--color-ivory);
  }

  .values-section__inner {
    width: min(calc(100% - 2rem), var(--content-max));
    margin-inline: auto;
    padding: clamp(2rem, 3vw, 3rem) 0 clamp(2.5rem, 3.5vw, 3.5rem);
  }

  .values-section__header {
    max-width: 820px;
    margin-bottom: clamp(2.5rem, 4vw, 3.5rem);
  }

  .values-section__eyebrow {
    margin-bottom: 1rem;
    color: var(--color-teal);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .values-section__title {
    font-size: clamp(2.7rem, 5vw, 5rem);
    line-height: 0.98;
    letter-spacing: -0.03em;
  }

  .values-section__title > span {
    color: var(--color-teal);
  }

  .values-section__dot,
  .value__dot {
    color: var(--color-coral);
  }

  .values-section__intro {
    max-width: 580px;
    margin-top: 2rem;
    font-size: 1.02rem;
    line-height: 1.7;
  }

  .values-section__grid {
    display: grid;
    gap: clamp(2.75rem, 5vw, 4.5rem);
  }

  .value {
    position: relative;
    max-width: 500px;
    min-width: 0;
  }

  .value__title {
    font-size: clamp(2.5rem, 4vw, 4.4rem);
    line-height: 0.95;
    letter-spacing: -0.025em;
  }

  .value__text {
    max-width: 390px;
    margin-top: 1rem;
    font-size: clamp(1rem, 1.25vw, 1.08rem);
    line-height: 1.7;
  }

  .values-section__thread {
    width: 100vw;
    margin-top: clamp(2.25rem, 3.5vw, 3.5rem);
    margin-left: calc((100vw - 100%) / -2);
  }

  .values-section__thread svg {
    display: block;
    width: 100%;
    height: 80px;
    overflow: visible;
  }

  .values-section__thread path {
    fill: none;
    stroke: var(--color-teal);
    stroke-width: 1.15;
    stroke-linecap: round;
    stroke-linejoin: round;
    vector-effect: non-scaling-stroke;
  }

  @media (min-width: 768px) {
    .values-section__inner {
      width: min(calc(100% - 4rem), var(--content-max));
    }

    .values-section__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      column-gap: clamp(5rem, 12vw, 12rem);
      row-gap: clamp(3rem, 4.5vw, 4.5rem);
    }

    .value:nth-child(2) {
      margin-top: 2rem;
    }

    .value:nth-child(4) {
      margin-top: 1.5rem;
    }

    .value:nth-child(1),
    .value:nth-child(3) {
      padding-left: 2rem;
    }
  }

  @media (min-width: 1200px) {
    .values-section__grid {
      padding-inline: clamp(1rem, 4vw, 4rem);
    }
  }

  @media (max-width: 767px) {
    .values-section__inner {
      padding: 2rem 0 0.5rem;
    }

    .values-section__header {
      margin-bottom: 2.25rem;
    }

    .values-section__title {
      font-size: clamp(2.6rem, 12vw, 3.6rem);
    }

    .values-section__grid {
      gap: 2.15rem;
    }

    .value:nth-child(1),
    .value:nth-child(2),
    .value:nth-child(3),
    .value:nth-child(4) {
      margin-top: 0;
      padding-left: 0;
    }

    .value__title {
      font-size: clamp(2.5rem, 12vw, 3.4rem);
    }

    .value__text {
      max-width: 100%;
      margin-top: 0.8rem;
      font-size: 0.98rem;
      line-height: 1.65;
    }

    .values-section__thread {
      margin-top: 1.5rem;
    }

    .values-section__thread svg {
      height: 44px;
    }
  }
</style>
