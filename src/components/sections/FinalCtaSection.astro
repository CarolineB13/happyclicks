<section class="final-cta">
  <div class="final-cta__inner">
    <p class="final-cta__eyebrow">On en parle ?</p>

    <h2 class="final-cta__title">
      Dites-moi ce que vous voulez<br />
      <span>
        construire<span class="final-cta__dot">.</span>
      </span>
    </h2>

    <p class="final-cta__text">
      Création, refonte ou besoin spécifique : quelques lignes suffisent pour me
      présenter votre projet. Je vous réponds avec une première orientation claire.
    </p>

    <a href="/votre-projet" class="final-cta__button">
      <span>Présenter mon projet</span>
      <span aria-hidden="true">→</span>
    </a>

    <div class="final-cta__thread" aria-hidden="true">
      <svg viewBox="0 0 1200 120" preserveAspectRatio="none">
        <path
          d="
            M -50 65
            C 110 28, 220 94, 390 61
            C 510 38, 575 38, 650 61
            C 690 74, 718 86, 742 65
            C 765 44, 735 29, 713 49
            C 687 72, 733 89, 790 64
            C 930 2, 1065 100, 1250 53
          "
        ></path>
      </svg>
    </div>
  </div>
</section>

<style>
  .final-cta {
    position: relative;
    overflow: hidden;
    background: var(--color-ink);
    color: var(--color-ivory);
  }

  .final-cta__inner {
    position: relative;
    width: min(calc(100% - 2rem), var(--content-max));
    margin-inline: auto;
    padding-block: clamp(3.75rem, 5vw, 5.5rem);
  }

  .final-cta__eyebrow {
    margin-bottom: 1.2rem;
    color: var(--color-teal);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .final-cta__title {
    max-width: 1050px;
    font-size: clamp(3rem, 6vw, 6.5rem);
    line-height: 0.95;
    letter-spacing: -0.035em;
  }

  .final-cta__title > span {
    color: var(--color-teal);
  }

  .final-cta__dot {
    color: var(--color-coral);
  }

  .final-cta__text {
    max-width: 620px;
    margin-top: 2.5rem;
    color: rgba(250, 247, 240, 0.82);
    font-size: clamp(1rem, 1.3vw, 1.15rem);
    line-height: 1.7;
  }

  .final-cta__button {
    display: inline-flex;
    align-items: center;
    gap: 1.3rem;
    margin-top: 2.5rem;
    min-height: 58px;
    padding: 0.95rem 1.8rem;
    background: var(--color-coral);
    border-radius: 999px 999px 999px 28px;
    color: var(--color-ivory);
    transition:
      transform var(--transition-base),
      box-shadow var(--transition-base);
  }

  .final-cta__button:hover,
  .final-cta__button:focus-visible {
    transform: translateY(-3px);
    box-shadow: 0 14px 35px rgba(231, 100, 79, 0.24);
  }

  .final-cta__button span:last-child {
    transition: transform var(--transition-base);
  }

  .final-cta__button:hover span:last-child,
  .final-cta__button:focus-visible span:last-child {
    transform: translateX(6px);
  }

  .final-cta__thread {
    width: 100vw;
    margin-top: clamp(3rem, 5vw, 4.5rem);
    margin-left: calc((100vw - 100%) / -2);
  }

  .final-cta__thread svg {
    display: block;
    width: 100%;
    height: 90px;
    overflow: visible;
  }

  .final-cta__thread path {
    fill: none;
    stroke: rgba(15, 159, 143, 0.75);
    stroke-width: 1.3;
    stroke-linecap: round;
    stroke-linejoin: round;
    vector-effect: non-scaling-stroke;
  }

  @media (min-width: 768px) {
    .final-cta__inner {
      width: min(calc(100% - 4rem), var(--content-max));
    }
  }

  @media (max-width: 767px) {
    .final-cta__inner {
      padding-top: 4rem;
      padding-bottom: 3rem;
    }

    .final-cta__title {
      font-size: clamp(2.8rem, 13vw, 4rem);
    }

    .final-cta__text {
      margin-top: 2rem;
      font-size: 0.98rem;
    }

    .final-cta__button {
      width: 100%;
      justify-content: space-between;
      margin-top: 2rem;
    }

    .final-cta__thread {
      margin-top: 2.75rem;
    }

    .final-cta__thread svg {
      height: 65px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .final-cta__button,
    .final-cta__button span:last-child {
      transition: none;
    }
  }
</style>
