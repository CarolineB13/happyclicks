<section class="projects-section" id="realisations">
  <div class="projects-section__inner">
    <header class="projects-section__header">
      <p class="projects-section__eyebrow">Réalisations</p>

      <h2 class="projects-section__title">
        Des besoins différents.<br />
        <span>
          Des solutions sur mesure<span class="projects-section__dot">.</span>
        </span>
      </h2>

      <p class="projects-section__intro">
        Deux activités, deux contextes, deux façons de construire une présence
        en ligne adaptée à ce qu’il faut réellement montrer et faciliter.
      </p>
    </header>

    <article class="project project--featured">
      <a
        href="/realisations/polymaintenance"
        class="project__visual project__visual--polymaintenance"
        aria-label="Découvrir le projet Polymaintenance"
      >
        <div class="project__device">
          <picture class="project__picture">
            <source
              srcset="/images/projects/polymaintenance-mobile.webp"
              media="(max-width: 767px)"
            />
            <img
              class="project__image"
              src="/images/projects/polymaintenance-desktop.webp"
              alt="Aperçu du site Polymaintenance"
            />
          </picture>
        </div>
      </a>

      <div class="project__content">
        <p class="project__type">Industrie · Site vitrine & négoce</p>
        <h3 class="project__title">Polymaintenance</h3>
        <p class="project__description">
          Repenser un site industriel pour rendre l’offre plus claire, renforcer
          la place du négoce et donner à l’entreprise une présence en ligne plus
          actuelle, tout en restant fidèle à son identité.
        </p>
        <a href="/realisations/polymaintenance" class="project__link">
          <span>Découvrir le projet</span>
          <span aria-hidden="true">→</span>
        </a>
      </div>
    </article>

    <article class="project project--beauty">
      <a
        href="https://www.appartbeauteinstitut.com/"
        target="_blank"
        rel="noopener noreferrer"
        class="project__visual project__visual--beauty"
        aria-label="Voir le site actuel d’Appart Beauté"
      >
        <div class="project__device">
          <picture class="project__picture">
            <source
              srcset="/images/projects/appart-beaute-current-mobile.webp"
              media="(max-width: 767px)"
            />
            <img
              class="project__image"
              src="/images/projects/appart-beaute-current-desktop.webp"
              alt="Aperçu du site actuel Appart Beauté"
            />
          </picture>
        </div>
      </a>

      <div class="project__content">
        <p class="project__type">Beauté · Bien-être</p>
        <h3 class="project__title">Appart Beauté</h3>
        <p class="project__description">
          Une première présence en ligne pour présenter l’institut, ses
          prestations et faciliter la prise de rendez-vous. Une refonte est
          prévue pour moderniser l’expérience et faire évoluer l’identité
          visuelle.
        </p>
        <div class="project__actions">
          <a
            href="https://www.appartbeauteinstitut.com/"
            target="_blank"
            rel="noopener noreferrer"
            class="project__link"
          >
            <span>Voir le site actuel</span>
            <span aria-hidden="true">↗</span>
          </a>
          <span class="project__status">Refonte à venir</span>
        </div>
      </div>
    </article>

    <div class="projects-section__footer">
      <a href="/realisations" class="projects-section__all">
        <span>Voir toutes les réalisations</span>
        <span aria-hidden="true">→</span>
      </a>
    </div>
  </div>
</section>

<style>
  .projects-section {
    position: relative;
    overflow: hidden;
    background: var(--color-ivory);
  }

  .projects-section__inner {
    width: min(calc(100% - 2rem), var(--content-max));
    margin-inline: auto;
    padding: clamp(2.5rem, 3.5vw, 3.5rem) 0 clamp(3rem, 4vw, 4rem);
  }

  .projects-section__header {
    max-width: 850px;
    margin-bottom: clamp(2.75rem, 4vw, 4rem);
  }

  .projects-section__eyebrow {
    margin-bottom: 1rem;
    color: var(--color-teal);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .projects-section__title {
    font-size: clamp(2.7rem, 5vw, 5rem);
    line-height: 0.98;
    letter-spacing: -0.03em;
  }

  .projects-section__title > span {
    color: var(--color-teal);
  }

  .projects-section__dot {
    color: var(--color-coral);
  }

  .projects-section__intro {
    max-width: 610px;
    margin-top: 1.7rem;
    font-size: 1.02rem;
    line-height: 1.7;
  }

  .project {
    display: grid;
    gap: 2.5rem;
    min-width: 0;
  }

  .project + .project {
    margin-top: clamp(4rem, 6vw, 6rem);
  }

  .project__visual {
    position: relative;
    display: block;
    min-width: 0;
    overflow: visible;
    color: inherit;
  }

  .project__device {
    position: relative;
    width: 100%;
    padding-top: 34px;
    overflow: hidden;
    background: #fff;
    border: 1px solid rgba(11, 37, 48, 0.12);
    border-radius: 18px 18px 50px 18px;
    box-shadow: 0 20px 50px rgba(11, 37, 48, 0.09);
  }

  .project__device::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 34px;
    background: var(--color-ivory);
    border-bottom: 1px solid rgba(11, 37, 48, 0.08);
  }

  .project__device::after {
    content: "";
    position: absolute;
    top: 14px;
    left: 16px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-coral);
    box-shadow:
      12px 0 0 rgba(15, 159, 143, 0.45),
      24px 0 0 rgba(11, 37, 48, 0.18);
  }

  .project__picture,
  .project__image {
    display: block;
    width: 100%;
  }

  .project__image {
    aspect-ratio: 16 / 9;
    object-fit: cover;
    object-position: top;
    transition: transform 600ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .project__visual:hover .project__image,
  .project__visual:focus-visible .project__image {
    transform: scale(1.012);
  }

  .project__content {
    align-self: center;
  }

  .project__type {
    margin-bottom: 0.7rem;
    color: var(--color-teal);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .project__title {
    font-size: clamp(2rem, 3vw, 3.4rem);
    line-height: 1;
  }

  .project__description {
    max-width: 480px;
    margin-top: 1.3rem;
    line-height: 1.7;
  }

  .project__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1rem 1.5rem;
    margin-top: 1.5rem;
  }

  .project__status {
    display: inline-block;
    padding: 0.45rem 0.75rem;
    background: var(--color-mist);
    border-radius: 999px;
    color: var(--color-teal);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .project__link {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    padding-bottom: 0.35rem;
    font-size: 0.92rem;
    font-weight: 500;
  }

  .project--featured .project__link {
    margin-top: 1.7rem;
  }

  .project__link::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: 0;
    width: 75%;
    height: 1px;
    background: var(--color-teal);
    transition: width var(--transition-base);
  }

  .project__link:hover::after,
  .project__link:focus-visible::after {
    width: 100%;
  }

  .project__link span:last-child {
    color: var(--color-teal);
    transition: transform var(--transition-base);
  }

  .project__link:hover span:last-child,
  .project__link:focus-visible span:last-child {
    transform: translateX(5px);
  }

  .projects-section__footer {
    display: flex;
    justify-content: flex-end;
    margin-top: clamp(3rem, 4vw, 4.5rem);
  }

  .projects-section__all {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    padding-bottom: 0.5rem;
    font-family: var(--font-heading);
    font-size: clamp(1.3rem, 2vw, 1.7rem);
    font-style: italic;
  }

  .projects-section__all::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: 0;
    width: 75%;
    height: 1px;
    background: var(--color-teal);
    transition: width var(--transition-base);
  }

  .projects-section__all:hover::after,
  .projects-section__all:focus-visible::after {
    width: 100%;
  }

  .projects-section__all span:last-child {
    color: var(--color-teal);
  }

  @media (min-width: 768px) {
    .projects-section__inner {
      width: min(calc(100% - 4rem), var(--content-max));
    }

    .project {
      grid-template-columns: minmax(0, 1.35fr) minmax(280px, 0.65fr);
      align-items: center;
      gap: clamp(3rem, 6vw, 6rem);
    }

    /* Appart Beauté reprend exactement la même composition que Polymaintenance :
       visuel à gauche, contenu à droite, avec les mêmes proportions. */
    .project--beauty .project__visual,
    .project--beauty .project__content {
      order: initial;
    }
  }

  @media (max-width: 767px) {
    .projects-section__inner {
      padding: 2.75rem 0 3.25rem;
    }

    .projects-section__title {
      font-size: clamp(2.6rem, 12vw, 3.6rem);
    }

    .project + .project {
      margin-top: 4.25rem;
    }

    .project__visual {
      padding-top: 0.5rem;
    }

    /* Même faux téléphone pour Polymaintenance et Appart Beauté. */
    .project__device {
      width: min(72%, 280px);
      margin-inline: auto;
      padding: 18px 7px 7px;
      background: var(--color-ink);
      border: 0;
      border-radius: 32px;
      box-shadow: 0 20px 42px rgba(11, 37, 48, 0.14);
    }

    .project__device::before {
      top: 7px;
      left: 50%;
      right: auto;
      width: 42px;
      height: 4px;
      background: rgba(250, 247, 240, 0.5);
      border: 0;
      border-radius: 999px;
      transform: translateX(-50%);
    }

    .project__device::after {
      display: none;
    }

    .project__picture {
      overflow: hidden;
      border-radius: 24px;
    }

    .project__image {
      aspect-ratio: 9 / 16;
      object-fit: cover;
      object-position: top;
      border-radius: 24px;
    }

    .projects-section__footer {
      justify-content: flex-start;
      margin-top: 3.5rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .project__image,
    .project__link span:last-child {
      transition: none;
    }
  }
</style>
