<header class="site-header">
  <div class="site-header__inner">
    <a href="/" class="site-header__logo" aria-label="Happyclicks — Accueil">
      <img src="/images/brand/happyclicks-logo-color.svg" alt="Happyclicks" />
    </a>
  </div>
</header>

<style>
  .site-header {
    position: relative;
    z-index: var(--z-navigation);

    width: 100%;
  }

  .site-header__inner {
    width: min(calc(100% - 2rem), var(--content-max));

    margin-inline: auto;

    padding-top: 1.25rem;
    padding-bottom: 0.5rem;
  }

  .site-header__logo {
    display: inline-flex;
    align-items: center;

    width: 140px;
  }

  .site-header__logo img {
    display: block;

    width: 100%;
    height: auto;
  }

  @media (min-width: 768px) {
    .site-header__inner {
      width: min(calc(100% - 4rem), var(--content-max));

      padding-block: 2rem;
    }

    .site-header__logo {
      width: 260px;
    }
  }
</style>
